// ONLINE_HEADERONLY_V1: implementation moved into mc_crypto.hpp (header-only) so
// it links without needing this file in the CMake source glob. This translation
// unit is intentionally just an include; if the build's glob DOES pick it up, the
// inline definitions from the header are harmless (COMDAT-folded, no duplicates).
#include "mc_crypto.hpp"
